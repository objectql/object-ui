# Dependencies
node_modules
.pnpm-store

# Production
dist
build
out

# Turbo
.turbo

# Misc
.DS_Store
.env
.env.local
.env.*.local
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage
.nyc_output

# IDEs
.idea
*.swp
*.swo

# OS
Thumbs.db

# TypeScript
*.tsbuildinfo

# ObjectUI specific
*.umd.js
*.esm.js

# VitePress build output
docs/.vitepress/dist
docs/.vitepress/cache

# TypeDoc generated API docs
docs/api

# Object UI CLI temporary files
.objectui-tmp

# ObjectStack bundled files
*.bundled_*.mjs

# Fumadocs
apps/site/.next
apps/site/.map.ts
apps/site/.source

# Next.js agent-rules files, minted by `next dev` in apps/site (objectui#4160).
# From next 16.3 (the #4094 bump; 16.2 bundled the docs but generated nothing),
# `next dev` detects an AI coding agent from the environment (CLAUDECODE,
# CURSOR, CODEX, AI_AGENT, ...) and writes AGENTS.md + CLAUDE.md next to the
# app's next.config — see node_modules/next/dist/server/lib/generate-agent-files.js.
# They appeared from merely RUNNING the app, were neither tracked nor ignored,
# and the block's own text says "committing it with your work keeps the tree
# clean": in a repo staged with `git add -A` by parallel agents that is a
# contamination trap of the objectui#3430 shape. We ignore rather than commit —
# the prose is tool-minted per next version, and apps/site/CLAUDE.md's
# `@AGENTS.md` import would splice framework rules into this repo's own binding
# instruction chain. `agentRules: false` in apps/site/next.config.mjs stops the
# minting at the source; these two lines are the belt to that braces, because
# the flag is upstream-owned and can be renamed or dropped by any next bump.
/apps/site/AGENTS.md
/apps/site/CLAUDE.md

# Test artifacts
test-screenshots
test-results
playwright-report

# Stray screenshots / traces in repo root (agents: save to /tmp/ instead)
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.heapsnapshot
/*.trace
# The extension-less shape the patterns above cannot catch: a root entry whose
# name starts with `--` is a CLI flag that got captured as an output filename
# (`... --full-page` written as `... > --full-page`, or a misordered argument),
# so it carries no extension to match on. Such a name is an argument-parsing
# accident by construction — nothing legitimate lives at the repo root under a
# flag name. See #3193, where a 68KB PNG literally named `--full-page` rode in
# with #3085 and reached every new worktree.
/--*

# Vite timestamp files
*.timestamp-*.mjs

# Backup files
*.bak
*.backup
*~

.objectstack

.playwright-mcp
# Live e2e auth state (contains a session token) + artifacts
e2e/live/.auth/
test-results/
playwright-report/
