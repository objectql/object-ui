---
---

docs(adr): ADR-0034 — propose unifying runtime metadata persistence (retire `sys_view`/`sys_report`/`sys_dashboard` in favour of a `user`-scoped metadata overlay). Documentation only; no package changes.
