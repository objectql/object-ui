---
---

Test-ledger-only change to `packages/types`'s `zod-mirror-parity` parity census: the `WiderThanDeclared` ledger gains a per-ARM classification table (`WIDER_ARMS`) with its arm enumeration measured from the mirrors, and the header gains the ledger's figures, pinned. No published source, no exports, no runtime behaviour — objectui#8252 classifies and re-files, it does not repair any divergence.
