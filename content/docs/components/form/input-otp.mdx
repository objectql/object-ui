---
title: "Input OTP"
description: "One-time password input with auto-focus"
---

import { DemoGrid } from '@/app/components/ComponentDemo';

The Input OTP component provides a secure way to enter one-time passwords or verification codes.

## Basic Usage

<SchemaExample id="components-form-input-otp/6-digit-otp" />

## With Separator

<SchemaExample id="components-form-input-otp/with-visual-separator" />

## Different Lengths

<DemoGrid>
  <SchemaExample id="components-form-input-otp/4-digit" />
  <SchemaExample id="components-form-input-otp/8-digit" />
</DemoGrid>

## Schema

```plaintext
interface InputOTPSchema {
  type: 'input-otp';
  length: number;                 // Number of digits
  value?: string;                 // Current value
  separator?: boolean;            // Show visual separator
  
  // Events
  onChange?: (value: string) => void;
  
  // States
  disabled?: boolean | string | { dialect?: string; source: string };   // boolean, predicate expression, or CEL envelope
  
  // Styling
  className?: string;
}
```

## Examples

### In a Form

<SchemaExample id="components-form-input-otp/verification-form" />
