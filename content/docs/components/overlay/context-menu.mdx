---
title: "Context Menu"
description: "Display a menu on right-click or long-press"
---

The Context Menu component displays a menu when right-clicking on an element.

## Basic Usage

<SchemaExample id="components-overlay-context-menu/basic-context-menu" />

## Schema

```plaintext
interface ContextMenuItem {
  label?: string;
  value?: string;
  icon?: string;
  type?: 'separator';
  disabled?: boolean;
}

interface ContextMenuSchema {
  type: 'context-menu';
  trigger: ComponentSchema;       // Trigger element
  items: ContextMenuItem[];       // Menu items
  onSelect?: string | ActionConfig;
  className?: string;
}
```
