---
title: "Menubar"
description: "Horizontal menu bar with dropdown menus"
---

The Menubar component provides a horizontal menu bar similar to desktop applications.

## Basic Usage

<SchemaExample id="components-overlay-menubar/application-menubar" />

## Schema

```plaintext
interface MenubarItem {
  label?: string;
  value?: string;
  icon?: string;
  shortcut?: string[];
  type?: 'separator';
  disabled?: boolean;
  onClick?: () => void;           // Item click handler
}

interface MenubarMenu {
  label: string;
  items: MenubarItem[];
}

interface MenubarSchema {
  type: 'menubar';
  menus: MenubarMenu[];           // Menu definitions
  
  // Styling
  className?: string;
}
```

Handlers are declared on the **item**, not on the menu: the shipped `MenuItem`
declares `onClick?: () => void`. The menu schema itself declares no event slot,
so there is no menu-level `onSelect`.
