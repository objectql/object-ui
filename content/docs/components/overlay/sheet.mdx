---
title: "Sheet"
description: "Slide-in panel from the edge of the screen"
---

import { DemoGrid } from '@/app/components/ComponentDemo';

The Sheet component displays content in a panel that slides in from the edge of the screen.

## Basic Usage

<SchemaExample id="components-overlay-sheet/basic-sheet" />

## Sides

<DemoGrid>
  <SchemaExample id="components-overlay-sheet/left-side" />
  <SchemaExample id="components-overlay-sheet/right-side" />
</DemoGrid>

## Schema

```plaintext
interface SheetSchema {
  type: 'sheet';
  trigger: ComponentSchema;       // Trigger component
  title?: string;                 // Sheet title
  description?: string;           // Sheet description
  content: ComponentSchema;       // Sheet content
  side?: 'left' | 'right' | 'top' | 'bottom';
  className?: string;
}
```
